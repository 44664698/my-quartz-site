# 1 评估函数 #常用函数 

去掉参数最外侧引号，并执行余下语句的函数。

格式：eval(`<`字符串或字符串变量>),

不能用于列表,列表转数字得先用[[split()]]把长字符串打断成列表格式,再用int等函数转数字.

例：>>>eval("1") 结果是：1 >>>eval("1+2") 结果是：3

eval(TempStr`[`0:-1]) `#`如果 TempStr`[`0:-1]值是“12.3”，结果是 12.3