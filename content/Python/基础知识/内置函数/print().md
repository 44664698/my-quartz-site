## 0.1 #常用函数 

### 0.1.1 以字符形式向控制台输出结果的函数，

### 0.1.2 格式：print(<拟输出字符串或字符串变量>)

#### 0.1.2.1 例：print("输入格式错误") `#`向控制台输出：输入格式错误

### 0.1.3 -print()函数的格式化：详见字符串的格式化

#### 0.1.3.1 print("轮换后的温度是{:.2f}C".format(C)) `#`{}表示槽，后续变量填充到槽中，C 填过来取 2 位小数

### 0.1.4 \r 光标回行首，\b 回退，\n 换行
```
str1="Hello World!"
str2="Hello Nowcoder!"
print(str1+"\n"+str2)
```

### 0.1.5 end=""，代替默认的换行符

#### 0.1.5.1 例：print("\r{:3}%".format(i),end="")