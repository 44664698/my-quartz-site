#函数和代码的复用 

## 0.1 方法论：基本代码抽象，即函数的使用方法

## 0.2 学会编写带有函数并复用代码的程序

## 0.3 函数的定义与使用

### 0.3.1 函数的理解与定义

#### 0.3.1.1 函数是一段完整代码的封装，具有特定功能，可利用的语句组

##### 0.3.1.1.1 例：def `<`函数名>(`<`n 个参数>):;`<`函数体>;return`<`返回值>

#### 0.3.1.2 y=f(x)

#### 0.3.1.3 参数是个占位符

#### 0.3.1.4 函数如不经调用，不会被执行

#### 0.3.1.5 参数是输入，内容是处理，return 是输出(IPO)

### 0.3.2 函数的使用及调用过程

#### 0.3.2.1 调用是运行函数代码的方式

#### 0.3.2.2 传递：调用时给出的参数，会替换定义中的参数![[Pasted image 20240217042159.png]]

#### 0.3.2.3 调用后得到返回值

### 0.3.3 函数的参数传递

#### 0.3.3.1 无参数：可以不给参数

##### 0.3.3.1.1 def fact():;print("我也是函数")

#### 0.3.3.2 可选参数：可给可不给，必须放在后面

##### 0.3.3.2.1 def `<`函数名>(`<`必选参数>,`<`可选参数>):;`<`函数体>;return`<`返回值>

###### 0.3.3.2.1.1 def fact(n,m=1):;return n//m

#### 0.3.3.3 可变数量参数：不确定多少个参数

##### 0.3.3.3.1 def `<`函数名>(`<`参数>,\*b):;`<`函数体>;return`<`返回值>

###### 0.3.3.3.1.1 def f(n,_x):;for y in x:;n_=y;return n

#### 0.3.3.4 按参数位置传递

##### 0.3.3.4.1 fact(10,5)

#### 0.3.3.5 按名称传递

##### 0.3.3.5.1 fact(m=5,n=10)

### 0.3.4 函数的返回值

#### 0.3.4.1 return 用来传递返回值，可以没有 return

#### 0.3.4.2 函数可以返回 n 个结果，n 可是为 0，可以为 1，可以为多

##### 0.3.4.2.1 a,b,c=f(10,5);return s//m,n,m

### 0.3.5 局部变量和全局变量

#### 0.3.5.1 局部变量是函数体内部变量

#### 0.3.5.2 全局变量整个程序使用的变量

##### 0.3.5.2.1 n=10;def f(n):

#### 0.3.5.3 局部变量不同于全局变量，即使同名

##### 0.3.5.3.1 print(f(n),n)

#### 0.3.5.4 函数内使用全局变量 global

##### 0.3.5.4.1 一次申明，往后皆为全局

###### 0.3.5.4.1.1 global s;print(f(s),s)结果相同

#### 0.3.5.5 当局部变量为组合数据类型且未在函数内被创建，等同于全局变量

##### 0.3.5.5.1 l=``[``"F","f"];def f(a):;l.append(a)结果全局变量 l 列表被修改

### 0.3.6 lambda 定义函数-慎用

#### 0.3.6.1 作为函数的参数来用

#### 0.3.6.2 `<`函数名>=lambda `<`参数>:`<`表达式>

##### 0.3.6.2.1 f=lambda x,y:x+y;f(10,15);25

##### 0.3.6.2.2 等价于 def `<`函数名>(`<`参数>):;`<`函数体>;return `<`返回值>

#### 0.3.6.3 无变量 lambda:f=lambda:"无变量"![[Pasted image 20240217042216.png]]

##### 0.3.6.3.1 print(f())结果：无变量

## 0.4 代码复用与函数递归

### 0.4.1 代码复用

#### 0.4.1.1 把代码当成资源进行抽象

#### 0.4.1.2 抽象级别不同，代码复用主要两种形式

##### 0.4.1.2.1 函数

###### 0.4.1.2.1.1 将代码命名，在代码层面建立初步抽象

##### 0.4.1.2.2 对象

###### 0.4.1.2.2.1 属性和方法，`<``a`>.`<``b`>和`<``a`>.`<``b`>()在函数之上再次组织进行抽象

### 0.4.2 模块化设计

#### 0.4.2.1 分而治之：通过函数或对象封装将程序划分为模块及模块间的表达

##### 0.4.2.1.1 主程序、子程序及子程序之间的关系

##### 0.4.2.1.2 子程序看作模块

##### 0.4.2.1.3 主程序看作模块之间的关系

##### 0.4.2.1.4 紧耦合：两个部分之间交流很多，无法独立存在

##### 0.4.2.1.5 松耦合：两个部分之间交流很少，可以独立存在

##### 0.4.2.1.6 模块内部紧耦合、模块之间松耦合，利于模块的通用化

### 0.4.3 函数递归的理解

#### 0.4.3.1 函数定义中调用函数自身的方式![[Pasted image 20240217042229.png]]

##### 0.4.3.1.1 基例：不需要再次递归的实例，一或多个![[Pasted image 20240217042239.png]]

##### 0.4.3.1.2 链条：计算过程存在递归链条![[Pasted image 20240217042248.png]]

#### 0.4.3.2 等价于往下阶乘![[Pasted image 20240217042303.png]]

### 0.4.4 函数递归的调用过程

#### 0.4.4.1 函数加分支语句![[Pasted image 20240217042312.png]]

### 0.4.5 实例解析

#### 0.4.5.1 斐波那契数列![[Pasted image 20240217042321.png]]

##### 0.4.5.1.1 f(n)=f(n-1)+f(n-2)![[Pasted image 20240217042331.png]]

#### 0.4.5.2 字符串 s 反转

##### 0.4.5.2.1 s=`[`::-1]![[Pasted image 20240217042340.png]]
