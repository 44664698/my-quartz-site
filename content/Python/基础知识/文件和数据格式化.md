---
number headings: auto, first-level 1, max 6, 1.1
---
#文件和数据格式化 

# 1 文件的使用

## 1.1 有统一编码：文本文件

### 1.1.1 用文本形式打开![[Pasted image 20240217042813.png]]

### 1.1.2 用二进制形式打开![[Pasted image 20240217042823.png]]

## 1.2 没有统一编码：二进制文件

## 1.3 文件处理步骤：打开>操作>关闭

### 1.3.1 打开 a=open(,)

![[Pasted image 20240217042838.png]]

##### 1.3.1.1.1 'r'默认值，只读模式，文件不存在则报错 FileNotFoundError

##### 1.3.1.1.2 'w'只写模式，不存在则创建，存在则覆盖

##### 1.3.1.1.3 'x'创建写，不存在则创建，存在则报错 FileExistsError

##### 1.3.1.1.4 'a'追加写，不存在则创建，存在则追加内容 ![[Pasted image 20240217042900.png]]

##### 1.3.1.1.5 'b'二进制文件模式

##### 1.3.1.1.6 't'默认值，文本文件模式

##### 1.3.1.1.7 '+'与 r/w/x/a 一同使用，增加同时读写功能

##### 1.3.1.1.8 encoding='utf-8'

### 1.3.2 `<`f>.read(size)

#### 1.3.2.1 读入全部内容，或参数位置前的内容

### 1.3.3 `<`f>.readline(size)

#### 1.3.3.1 读入一行内容，如果给出参数，读入该行前 size 长度

### 1.3.4 `<`f>.readlines(hint)

#### 1.3.4.1 读入所有行，或参数前所有行，以每行为元素形成列表

### 1.3.5 `<`f>.write(s)

#### 1.3.5.1 向文件写入一个字符串或字节流

### 1.3.6 `<`f>.writelines(lines)

#### 1.3.6.1 将列表写入文件，列表全元素为字符串![[Pasted image 20240217042919.png]]

### 1.3.7 `<`f>.seek(offset)

#### 1.3.7.1 操作光标(指针)位置，0 开头，1 当前，2 结尾

##### 1.3.7.1.1 f.seek(0)#回到文首

##### 1.3.7.1.2 没有 seek 就打印为空，因光标在文尾![[Pasted image 20240217035757.png]]

### 1.3.8 `<`f>.close()

### 1.3.9 开关框架 with open(,) as f:

#### 1.3.9.1 [python 使用 with open() as 读写文件的操作方法*python*脚本之家 (jb51.net)](https://www.jb51.net/article/267550.htm)

##### 1.3.9.1.1 with open('file','r') as f:

##### 1.3.9.1.2 print(f.read())

##### 1.3.9.1.3 `#`等价于：

##### 1.3.9.1.4 try:

##### 1.3.9.1.5 f=open('file','r')

##### 1.3.9.1.6 print(f.read())

##### 1.3.9.1.7 f.close()

##### 1.3.9.1.8 finally:

##### 1.3.9.1.9 if f:#如果读写时出错，导致文件没有关闭

##### 1.3.9.1.10 f.close()

## 1.4 全文本操作

### 1.4.1 txt=f.read()

#### 1.4.1.1 一次全处理，越大越慢

#### 1.4.1.2 txt=f.read(2);while txt!="";#文本处理;txt=f.read(2)

#### 1.4.1.3 按量读入，逐步处理，光标渐进

### 1.4.2 for line in f.readlines():

#### 1.4.2.1 一次读入，分行处理

### 1.4.3 for line in f:

#### 1.4.3.1 分行读入，逐行处理

# 2 实例：自动轨迹绘制

## 2.1 概念

### 2.1.1 用程序读取数据来绘制图形

### 2.1.2 用编制数据的方式来绘制轨迹

### 2.1.3 数据+脚本是自动化最重要的第一步

## 2.2 思路

### 2.2.1 定义数据文件的格式（接口）

### 2.2.2 编写程序，根据文件接口解析参数绘制图形

### 2.2.3 编制数据文件

## 2.3 实例

### 2.3.1 map(函数名 f,组合 l)

#### 2.3.1.1 将参数 1(函数 f)的功能作用于参数 2(组合 l)的每一个元素![[Pasted image 20240217043103.png]]

## 2.4 方法思维

### 2.4.1 自动化思维：数据和功能分离，数据驱动的自动运行

### 2.4.2 接口化设计：格式化设计接口，清晰明了

### 2.4.3 二维数据应用：应用维度组织数据，二维数据最常用

# 3 数据的维度

^413cdc

## 3.1 一个数据

## 3.2 一组数据

## 3.3 一维

### 3.3.1 由对等关系的有序或无序数据构成，采用线性方式组织。

### 3.3.2 对应列表(有序/数据类型可不同)、数组(同类型)和集合(无序)

## 3.4 二维

### 3.4.1 由多个一维数据构成，是一维数据的组合形式

### 3.4.2 表格是典型的二维数据，表头可以是或不是二维数据的一部分

### 3.4.3 对应列表，列表中的元素也是列表

## 3.5 多维数据

### 3.5.1 由一维或二维数据在新维度上扩展形成
### 3.5.2 对应列表，列表中的元素也是列表

### 3.5.3 如年度表格的不同年份

## 3.6 高维数据

### 3.6.1 对应字典类型

### 3.6.2 仅用最基本的二元关系，来展示数据间的复杂结构
![[Pasted image 20240217043121.png]]
# 4 数据格式

国际公认：JSON、XML、YAML等

# 5 一维数据的格式化和处理
### 5.1.1 数据的操作周期

#### 5.1.1.1 数据操作：操作方式(算法)

#### 5.1.1.2 数据存储：存储格式

#### 5.1.1.3 数据表示：数据类型

## 5.2 一维数据的表示

### 5.2.1 有序：用列表类型

#### 5.2.1.1 for 循环可遍历，进而处理

### 5.2.2 无序：用集合

#### 5.2.2.1 for 循环可遍历，进而处理

## 5.3 一维数据的存储

### 5.3.1 1、空格分隔

#### 5.3.1.1 使用一个或多个空格分隔存储，不换行

#### 5.3.1.2 缺点：数据内部不能存在空格

### 5.3.2 2、逗号分隔

#### 5.3.2.1 缺点：数据内部不能有英文逗号

#### 5.3.2.2 用英文逗号分隔数据进行存储，不换行

### 5.3.3 3、其他方式，如\$

#### 5.3.3.1 缺点：需要根据数据特点定义，通用性较差

#### 5.3.3.2 用其他符号或符号组合来分隔，建议用特殊符号

## 5.4 一维数据的处理

### 5.4.1 存储格式与表示方式(集合或列表)的转化

#### 5.4.1.1 将存储的数据读入程序 open![[Pasted image 20240217043206.png]]

#### 5.4.1.2 将程序表示的数据写入文件.write![[Pasted image 20240217043215.png]]

# 6 二维数据的格式化和处理

## 6.1 二维数据的表示

### 6.1.1 (二维)列表类型![[Pasted image 20240217043224.png]]

#### 6.1.1.1 外层列表中每个元素对应一行或一列

#### 6.1.1.2 使用 for 循环遍历每个元素

## 6.2 CSV 数据存储格式

### 6.2.1 国际通用的一二维数据存储格式，一般.csv 扩展名

### 6.2.2 每行一个一维数据，逗号分隔，无空行

### 6.2.3 Excel 和一般编辑软件都可以读入或另存为 CSV 文件

### 6.2.4 规则

#### 6.2.4.1 若缺失某个元素，逗号要保留

#### 6.2.4.2 二维数据的表头可作为数据存储，或另行存储

#### 6.2.4.3 英文逗号，逗号与数据之间无额外空格

#### 6.2.4.4 数据中出现逗号，1、加引号，2、加转义符

## 6.3 二维数据的存储

### 6.3.1 按行存？按列存？

#### 6.3.1.1 都可以，由程序决定

#### 6.3.1.2 一般索引习惯：ls``[``row]``[``column]，先行后列

#### 6.3.1.3 根据一般习惯，外层列表每个元素是一行，按行存

## 6.4 二维数据的处理

### 6.4.1 将 CSV 文件读入二维列表

![[Pasted image 20240217043243.png]]

### 6.4.2 将数据写入 CSV 文件

![[Pasted image 20240217043305.png]]

### 6.4.3 二层循环，逐一处理

![[Pasted image 20240217043319.png]]

# 7 wordcloud 库的使用

## 7.1 wordcloud 库把词云当作一个 WordCloud 对象

### 7.1.1 -wordcloud.WordCloud()代表一个文本对应的词云

### 7.1.2 -可以根据文本中词语出现的频率等参数绘制词云

### 7.1.3 绘制词云的形状、尺寸和颜色都可以设定

## 7.2 w=wordcloud.WordCloud()

### 7.2.1 -以 WordCloud 对象为基础

### 7.2.2 -配置参数、加载文本、输出文件

### 7.2.3 w.generate(txt)

#### 7.2.3.1 向 WordCloud 对象 w 中加载文本 txt

##### 7.2.3.1.1 >>>w.generate("python")

### 7.2.4 w.to_file(filename)

#### 7.2.4.1 将词云输出为图像文件，.png 或.jpg 格式

##### 7.2.4.1.1 >>>w.to_file("outfile.png")

## 7.3 wordcloud 库常规方法

### 7.3.1 1、配置对象参数

#### 7.3.1.1 impot wordcloud;c=wordcloud.WordCloud()

### 7.3.2 2、加载词云文本

#### 7.3.2.1 c.generate("hi")

#### 7.3.2.2 中文：空格 join 分词

##### 7.3.2.2.1 w.generate(" ".join(jieba.lcut(txt)))

### 7.3.3 3、输出词云文件

#### 7.3.3.1 c.to_file("词云.png")

## 7.4 wordcloud 后台做的事

### 7.4.1 分隔：以空格分隔单词

### 7.4.2 统计：单词出现次数并过滤(很短的词过滤掉)

### 7.4.3 字体：根据统计结果配置字号

### 7.4.4 布局：颜色环境尺寸

## 7.5 配置对象参数

### 7.5.1 width=xxx 指定词云对象生成图片的宽度，默认 400 像素

#### 7.5.1.1 w=wordcloud.WordCloud(width=600)

## 7.6 height=xxx 指定词云对象生成图片的高度，默认 200 像素

### 7.6.1 w=wordcloud.WordCloud(height=400)

### 7.6.2 min_font_size=xx 指定词云中字体的最小字号，默认 4 号

### 7.6.3 max_font_size=xx 指定词云中字体的最大字号，根据高度自动调节

### 7.6.4 font_step=xx 指定词云中字体字号的步进间隔，默认为 1

### 7.6.5 font_path="\*.ttc"指定字体文件的路径，默认为 None(系统默认字体)

#### 7.6.5.1 w=wordcloud.WordCloud(font_path="msyh.ttc")

### 7.6.6 max_words 指定词云显示的最多单词数量，默认 200

#### 7.6.6.1 w=wordcloud.WordCloud(max_words=20)

### 7.6.7 stop_words 指定词云排除词列表(一般用集合类型)

#### 7.6.7.1 w=wordcloud.WordCloud(stop_words=("hi")

### 7.6.8 mask 指定词云形状，默认长方形，需要引用 imread()函数

#### 7.6.8.1 from scipy.misc import imread

#### 7.6.8.2 mk=imread("pic.png")#背景是白色的实心图形文件

#### 7.6.8.3 w=wordcloud.WordCloud(mask=mk)又(mask=mask)

![[Pasted image 20240217043344.png]]
![[Pasted image 20240217043352.png]]

### 7.6.9 background_color 指定词云图片的背景颜色，默认黑色

#### 7.6.9.1 w=wordcloud.WordCloud(background_color="white")

# 8 实例：政府工作报告词云

## 8.1 基本思路

### 8.1.1 1、读取文件、分词整理

### 8.1.2 2、设置并输出词云

### 8.1.3 3、观察结果，优化迭代

