- 镜像采用配置之间的实体镜像还是文件的衡量：
- 实体镜像还是零件镜像：
	- 实体镜像不会同时镜像它所在零件的MBD尺寸。实体镜像与零件镜像是两种不同的操作。实体镜像只会镜像三维模型的形状和几何特征，而零件镜像会镜像整个零件及其属性，包括零件的名称、材料、MBD尺寸等。因此，如果需要同时镜像模型和它的MBD尺寸，最好采用零件镜像操作。
- 虽然实体镜像不能保留尺寸，但工程图有镜像视图的功能。
	- 虽然实体镜像不能分离文件名，但是图号、名称等属性都可以在ZTOOL里分别赋予。
- 镜像成零件，意味着文件系统变复杂。
- 结论：实体镜像，配置分离，文件不分离。